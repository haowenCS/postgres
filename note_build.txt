mkdir build
cd build
../configure --prefix=/home/hw470/Documents/Work/db/postgres/build
sudo make
sudo make install

# create the data directory
mkdir data
./bin/initdb ./data/

# start the server
./bin/pg_ctl -D ./data/ start

20230114:
ERROR: could not bind IPv4 address "127.0.0.1": Address already in use
Solution:
Change the port number for the server in the postgresql.conf file
position: ./data/postgresql.conf port->5433

# create a default user
./bin/createuser -s -r postgres

# connect to server
./bin/psql postgres

# db user
\du

# use db
create database testdb;
\l # list db
\c testdb # connect
 insert into t1(id, name) values(1, 'hao'), (2, 'wen');
 select * from t1;


# resources:
https://postgresqltutorial.com/
